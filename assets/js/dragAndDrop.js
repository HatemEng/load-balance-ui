/*
let allowDrop = (ev) => {
    ev.preventDefault();
};

let drag = (ev) => {
    console.log(ev);
    ev.target.style.background = '#c1c1c1';
    ev.dataTransfer.setData("text", ev.target.id);
};

let drop = (ev) => {
    ev.preventDefault();
    var data = ev.dataTransfer.getData("text");
    ev.target.appendChild(document.getElementById(data));
};
*/
$(document).ready(() => {
    $(function () {
        //$("#source-data-holder").draggable();
        //$("#p-src-1").draggable();

    });

});
